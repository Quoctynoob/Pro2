import React from "react";

const Hero = () => {
    return (
        <section id="hero" className="bg-green-50 w-full h-screen">
            <div className="text-2xl">
                Game, Set, Match!
            </div>
        </section>
    );
};

export default Hero;